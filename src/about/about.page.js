import React from 'react'
import Layout from '../shared/ui/layout'

const About = (props) => {
    return (
        <Layout {...props}>
            About
        </Layout>
    )
}
export default About